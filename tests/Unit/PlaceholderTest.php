<?php

test('placeholder test to ensure Unit directory is not empty', function () {
    // This is a placeholder test to ensure the Unit directory is not empty
    // and can be found by the test runner
    expect(true)->toBeTrue();
});
